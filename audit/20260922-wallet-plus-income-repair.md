# 钱包 Plus 历史收益生产修复记录

2026-09-22 09:48 完成代码上线与补账，随后完成线上只读核对（北京时间）。

## 结果

钱包 2026-09-19、20、21 日接近零的收益已修复，每日显示 0.68 元。线上历史明细、组合接口与数据库一致。

| 日期 | 修复前收益（元，约） | 修复后收益（元） |
| --- | ---: | ---: |
| 2026-09-19 | 0.0000246345623 | 0.6759935581623316725251132835 |
| 2026-09-20 | 0.000000000897683 | 0.6760181913675911078912600985 |
| 2026-09-21 | 0.000000000897716 | 0.6760428254704845429112316561 |

- 钱包余额：`124367.9260428254704845429112` 元，显示 **124,367.93 元**。
- 页面累计收益：`100.2045063276817696308997934` 元，显示 **100.20 元**，等于 8 月起每日收益明细之和。
- 含人工校准的账本累计收益：`98.85604282547048454291123166` 元。这与页面每日收益合计采用不同口径，已分别验证。
- 当前钱包在途金额为零。

9 月 21 日用户已将持仓校准为 128367.25 元、账本累计收益校准为 98.18 元。修复保留原校准记录，并各补记 `-1.352011749529922780416373382` 的持仓/收益校准差额，抵消该校准之前的 19、20 日新增收益。21 日收益正常增加一次，因此本次钱包余额净增加约 0.6760181891 元，没有重复增加已校准金额。

## 代码修复

根因是历史计息投影优先消耗已确认本金，页面在途余额却优先消耗待确认资金。现统一采用待确认批次分配，确认时只计入该批次尚未消耗的份额。保留原有早期导入流水顺序，并覆盖退款、跨确认日和双重冲正后的有效流水。

普通收益调度继续保持幂等。历史修复使用独立工具：先在数据库副本中预演，再校验数据库指纹，在单一事务中冲正原收益、按日期重算并记录审计。原金额不覆盖；同一计划重复执行不会新增流水，重叠修复区间会被拒绝。

部署提交：`2212b0e54545059fa29984cd6edb1ac1b96224fa`。生产部署状态已记录该提交，`source_dirty=false`。共上传六个运行文件：

1. `src/portfolio_wallet_allocation.py`
2. `src/portfolio_positions.py`
3. `src/portfolio_view.py`
4. `src/portfolio_income.py`
5. `src/portfolio_income_repair.py`
6. `scripts/repair_wallet_plus_income.py`

## 验证与审计

- 最终完整测试：`1059 passed in 70.46s`；Python 编译、Git 空白检查通过。
- `[Codex / gpt-5.6-sol]` 最终复核无阻断问题，13 项历史修复测试通过。
- 在生产快照上验证逐日复利、人工校准、异常事务回滚、重复修复、普通调度重试，以及所有产品持仓与账本投影一致。
- 生产停服后通过 SQLite backup 创建一致备份。现场重新生成的预演结果与已验证快照完全一致后才执行。
- 补账操作 ID：`c20f1b30-3e35-553f-a346-6b6487a3a9df`。
- 原三条收益仅更新冲正状态，原始金额与其他字段保留。新增三条冲正、三条重算收益、两条校准补偿，共八条钱包流水；新增两条审计。其他表、其他产品持仓和原审计记录保持不变。
- 停服核对中所有产品持仓均与账本投影一致；同一生产计划重复执行返回 `already_repaired`，数据库内容不变。
- 重启后，三天各有一条有效收益和一条已冲正原收益；SQLite 完整性检查为 `ok`，外键违规为零。
- 6 个文件的本地/线上 SHA256 全部一致；服务 `daily-report` 为 `active`。
- `/`、`/nav`、静态资源以及鉴权 `/api/nav-dashboard`、`/api/portfolio`、钱包历史接口均返回 HTTP 200。
- 修复后 09:48:45 的正常组合周期完成，`quotes=22 intents=0 settled=0 redemptions=0 income=15`；该周期后的流水检查无重复入账。

## 备份与证据

生产代码备份：`/home/ubuntu/daily_report/backups/20260922_wallet_income_code_final`

生产数据备份与执行计划：`/home/ubuntu/daily_report/backups/20260922_wallet_income_final_data`，内含 `portfolio.before.db`、`repair-plan.json`、`repair-result.json`。

本地验证证据保存在已忽略目录 `runtime/wallet-income-repair-20260922/`：生产原始快照、完整部署日志、快照验证结果、维护脚本、生产补账日志和线上验证结果。没有上传本地配置、密钥、数据库或测试文件。

访问地址：[生产持仓页面](http://8.213.145.226:8080/nav)。
